services:
  card-prediction:
    gpus: "all"  # Compose GPU request (requires NVIDIA Container Toolkit)
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
